<section class="py-16 sm:py-24">
  <div class="container-brand max-w-2xl mx-auto">
    <p class="eyebrow mb-4 text-center">{{ page.title }}</p>
    <h1 class="font-serif text-3xl sm:text-4xl text-ink text-center mb-10">{{ page.title }}</h1>
    <div class="text-[15px] leading-relaxed text-ink-soft creme-richtext">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{ "name": "Página", "settings": [] }
{% endschema %}
